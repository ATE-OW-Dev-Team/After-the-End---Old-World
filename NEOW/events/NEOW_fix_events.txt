namespace=fixevent

character_event = { 
	id = fixevent.1
	hide_window = yes
	
	trigger = {
		trait = legendary 
		is_alive = yes
		is_save_game = no
	}
	mean_time_to_happen = {
		days = 1
	}
	immediate = {
		remove_trait = legendary
	}
}
